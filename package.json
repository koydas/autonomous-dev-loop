{
  "name": "autonomous-dev-loop",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test scripts/tests/*.test.mjs",
    "lint": "bash -lc 'for f in scripts/*.mjs scripts/lib/*.mjs; do node --check \"$f\"; done'"
  }
}
